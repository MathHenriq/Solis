# Backend do Solis. Só o necessário pra falar HTTP e servir HTTP.
# Nada de LangChain e afins: a conversa com o Ollama é um POST em /api/chat, e
# uma camada de abstração em cima disso custaria megabytes e travaria a versão
# de metade da árvore de dependências.
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27

# Base de conhecimento. Leitura de arquivo e upload multipart — a indexação
# (chunking, FTS5, ranqueamento) é código do projeto, não biblioteca.
pypdf>=5.1
python-docx>=1.1
python-multipart>=0.0.20
